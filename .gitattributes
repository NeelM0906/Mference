# The vendor chat template is kept verbatim (its SHA-256 is pinned in the
# fixture); it carries trailing whitespace that git diff --check would flag.
Tests/Mference/Core/Tokenization/Fixtures/MiniCPM5Tokenizer/chat_template.jinja -whitespace
